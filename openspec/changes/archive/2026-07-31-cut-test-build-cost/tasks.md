## 1. Baseline measurement

- [x] 1.1 Record the pre-change clean-build baseline from a full `Rebuild Project` (keeps `CMakeCache.txt`; a cache wipe would lose the hand-set `DECENZA_MACOS_CODESIGN_IDENTITY` and `QMLLINT_SKIP_UNLINTABLE=OFF`). **Done — 4,846 s cpu total, `tests/` 1,956 s / 40%, link 3.0%; see `baseline.md`.**
- [x] 1.2 Record the pre-change incremental baseline: touch `src/controllers/profilemanager.cpp`, rebuild, capture cpu. **Done — 92.3 s cpu total, 77.0 s attributable to `tests/` (9 compiles at 6.79 s, 9 relinks at 1.42 s). The "today" and "`decenza_testlib`" predictions held (78/67 vs 77.0/~67.0); the narrow-library prediction was optimistic — ~23 s, not 13 s, because it used the 0.53 s all-target link mean where these nine heavy targets relink at 1.42 s. Conclusion unchanged: narrow library saves 54 s here, `decenza_testlib` saves 10 s. Post-change re-measurement is task 2.7.**
- [x] 1.3 Record the July 2026 test-lines-per-source-line ratio (0.637) and the exact command, so the September check is comparable rather than re-derived differently. **Done — see `baseline.md`.**

## 2. Deduplicate production sources across test targets

- [x] 2.1 Enumerate every production source compiled into two or more test targets, with target lists and cpu. **Done — 46 duplicate object files, 215 s, 17 sources. Two findings that shape 2.2–2.3: the three largest sources share one identical 9-target set and the two 3-target sets are subsets of it, so three libraries cover 175 s of the 215 s; and `src/ai/shotanalysis.cpp` is already in `decenza_testlib` and compiled again by `tst_visualizershotparse`, which is a source-list mistake, not a case for a library.**
- [x] 2.2 Remove the `shotanalysis.cpp` entry from `tst_visualizershotparse` — it already links `decenza_testlib`. **Done.**
- [x] 2.3 Add a narrow library for the set-A sources. **Done — `decenza_profilelib`. The `PROFILEMANAGER_SOURCES` variable was already set A exactly, pasted into nine targets. Surfaced a hidden requirement: `profilesavehelper.cpp` reaches `MQTTAsync.h` through `maincontroller.h`, which was invisible while every consumer linked paho itself; the library links `paho-mqtt3a-static` for its include dir. Incremental measured after: touching `profilemanager.cpp` fell from 77.0 s to 12.2 s test-attributable — better than the ~23 s predicted, because the single remaining compile runs uncontended (2.82 s) where nine parallel ones averaged 6.79 s.**
- [x] 2.4 Narrow libraries for the remaining 3+ consumer sets. **Done — `decenza_weightlib` (weightprocessor + stepexitarbiter, 11.1 s), `decenza_a11ylib` (accessibilitymanager, 12.7 s), `decenza_appserviceslib` (screensavervideomanager + batterymanager, 15.2 s), `decenza_beanbaselib` (7.8 s). Set B (`mcpserver.cpp`, 14.9 s) deferred to 2.5 — it is entangled with `MCP_MOC_HEADERS` and `version_code.cpp`.**
  - **Correction worth keeping: merging a11y and appservices into one library FAILED to link.** The merge was justified in a comment by "a static library contributes only the objects the executable actually references" — false when the sources carry `Q_OBJECT`. AUTOMOC emits ONE `mocs_compilation.cpp` per target holding every class's meta-object, so pulling it in for `AccessibilityManager` dragged `ScreensaverVideoManager` with it and `tst_accessibility_announcements` failed on undefined `ProfileStorage::staticMetaObject`. Libraries whose consumer sets differ must stay separate whenever the sources are `Q_OBJECT`. The comment in `tests/CMakeLists.txt` now records this.
- [x] 2.5 Decide the remaining cases per the design's open question: `accessibilitymanager.cpp` (4 targets), `beanbaseclient.cpp` (3), and the seven two-consumer pairs. Either they move, or the target's list drops the source, or the check gets a documented exception naming the reason. Leaving one in place with the check failing is not an option. **Done — seven more narrow libraries (`decenza_mcpserverlib`, `decenza_aiproviderlib`, `decenza_dialingblockslib`, `decenza_settingsserializerlib`, `decenza_webdebugloggerlib`, `decenza_wifiscaleresultlib`, `decenza_refractometerlib`). No exception list: each two-consumer case saves only 2-6 s, but the point is that the gate can be absolute. A rule with no knobs cannot drift. `check_test_source_duplication.py` now reports 0 duplicates across 105 targets; suite 110/110 green.**
  - Two traps hit while doing it. **`${MCP_MOC_HEADERS}` must NOT go in the library** — those headers are moc'd by targets that do not link it, producing 24 duplicate symbols (`McpToolRegistry`, `McpResourceRegistry`, `MockTransport`). A moc'd header belongs to exactly one compilation unit per link; hoist one only when every user links that library. **And the libraries must be spelled out literally, never generated in a `foreach()`** — `check_test_source_duplication.py` reads this file as text (it runs in the build-free PR job), so a configure-time library name is invisible to it. A `foreach()` version was written and reverted for exactly that reason.
- [x] 2.6 Add a comment in `tests/CMakeLists.txt` stating why deduplication goes into narrow libraries rather than `decenza_testlib`, with the incremental arithmetic from design.md. Without it the next person will "simplify" this back into the shared library. **Done — the arithmetic, the AUTOMOC/Q_OBJECT merge trap, the moc'd-header rule, the OBJECT-vs-STATIC rule for generated qrc, and the keep-it-greppable rule are all in the file next to the code they govern.**
- [x] 2.7 Verify: clean rebuild, diff `.ninja_log` against `baseline.md` — duplicate object files should be 0 and `tests/` cpu down by ~215 s. **Done — duplicate object files 46 → 0, confirmed independently by `.ninja_log` and by `check_test_source_duplication.py`.** Incremental re-measured: 77.0 s → **12.2 s** test-attributable, one compile instead of nine.

- [x] 2.8 **ATTEMPTED AND REVERTED.** Stop `maincontroller.h` propagating the third-party `<MQTTAsync.h>` to its 16 includers. It uses `MqttClient` **only as a pointer** (`Q_PROPERTY` at :180, getter at :273, member at :714), so forward-declare it in the header and move `#include "../network/mqttclient.h"` into `maincontroller.cpp`. Do not pimpl `mqttclient.h` itself — it uses `MQTTAsync_successData*` and friends in real member declarations, so its own include is legitimate; the fix is to stop propagating it. Only 3 files use `MqttClient`'s API and 2 already include it directly. Found by hoisting `profilesavehelper.cpp` into `decenza_profilelib` (task 2.3), where the requirement stopped being invisible.
  - **Not viable. `Q_PROPERTY(MqttClient* mqttClient ...)` requires the complete type** — Qt's metatype system rejects an incomplete pointee ("Pointer Meta Types must either point to fully-defined types or be declared with `Q_DECLARE_OPAQUE_POINTER`"). The opaque-pointer escape hatch is worse than the include: `SettingsHomeAutomationTab.qml` reads through this property at 8 sites (`MainController.mqttClient.connected` / `.status` / `.connectToBroker()`), and an opaque pointer has no introspectable members, so all 8 would degrade at RUNTIME rather than fail to compile. The predicted `main.cpp` fallout (5 call sites) was real and trivial; the `Q_PROPERTY` is what kills it.
  - **Measured before reverting, as required: touching `mqttclient.h` rebuilds 65 edges / 26 compiles / 170 s cpu.** Real, bounded, and not worth a silent runtime regression. `maincontroller.h` now carries a comment recording all of this so the idea is not retried from scratch.
  - **Measure it, do not assume.** Touch `src/network/mqttclient.h`, rebuild, diff `.ninja_log` — before and after, same method as 1.2. "Fat header, obviously bad" is exactly the plausible-sounding claim this change refuses everywhere else. If the saving turns out trivial, **say so and keep the change anyway** on include-hygiene grounds, rather than reporting it as a build win it was not.
  - Watch for: callers relying on the transitive include (the compiler finds them; each needs its own `#include`), and that the QML registration path still resolves `MqttClient` as a type with only a forward declaration in `maincontroller.h`.

## 3. Deduplicate compiled Qt resources

- [x] 3.1 Consolidate `qrc_resources.cpp` (compiled in three test targets, 22.4 s of it duplicate) into a single compilation linked by those targets. **Done — `decenza_testresources`, an **OBJECT** library. STATIC would not work: nothing references a symbol in a generated resource `.cpp` (it is reached only via its static initialiser), so a linker has no reason to pull it from an archive and the resource silently fails to register. The three consuming tests assert resources actually resolve, so a mistake here fails loudly rather than silently — they pass.**
- [x] 3.2 Look once at whether `tst_emojiassets` needs the compiled emoji resource at all (17.7 s, the largest single edge under `tests/`) or can assert against the source tree via `DECENZA_SOURCE_DIR` as other tests do. **Finding: it is genuinely required — keep it.** The existing comment gives the reason and it holds: compiling `emoji.qrc` in asserts what actually SHIPS, and without it a stale or missing qrc would silently strip every emoji in the app while `tst_textescaping` (which reads the source directory) stayed green. 17.7 s is the price of that assertion. Not duplicated — just the largest single edge.
- [x] 3.3 Verify: clean rebuild, qrc down. **Done — qrc objects under `tests/` 14 → 12, 54 s → 28 s (−26 s). `qrc_resources.cpp` now compiled once instead of three times.**

## 4. Enforce the rule on every pull request

- [x] 4.1 Write `scripts/check_test_source_duplication.py`: parse `tests/CMakeLists.txt` as text, fail on a production source listed by two or more test targets, and fail on a source listed by a target that `decenza_testlib` already compiles. No Qt, no configured build dir, no compilation. **Done.**
- [x] 4.2 Verify the check FAILS on the pre-change tree. **Done — run against `git show HEAD:tests/CMakeLists.txt`, it reports 16 shared sources / 45 redundant compiles plus the 1 `shotanalysis.cpp` already-in-a-library case. That is 17 sources / 46 compiles, matching the independent `.ninja_log` measurement exactly. Two unrelated methods agreeing is the reason the number is trustworthy; neither alone would be.**
- [x] 4.3 Verify it passes after 2.5, **Done — `check_test_source_duplication: OK — no production source is compiled into more than one of the 105 test targets.` Its failure message names the source and every target involved, verified against the pre-change tree.**
  - **A parser bug was found in the check itself, by its own numbers.** Recognising `OBJECT` libraries (needed once `decenza_testresources` existed) made single-line `add_library(foo STATIC .../x.cpp)` calls swallow the block after them, because the block reader scanned ahead for a standalone `)`. The only visible symptom was the reported target count dropping 105 → 104. **Under-parsing fails OPEN** — fewer targets means fewer duplicates found, so an incomplete parse looks exactly like success. Fixed, and the parsed count is now cross-checked against a naive `grep -c '^add_decenza_test('` (106 = 106) with a hard refusal on mismatch. The guard was verified by making the parser drop a target and watching it refuse, not by watching it pass. Originally blocked on 2.5, which reported the 7 real duplicates that remained (8 redundant compiles, matching ninja exactly). Also confirm and that the failure message names the source and every target involved — a message naming only one target sends the reader to the wrong file.
- [x] 4.4 Wire it into `.github/workflows/text-invariants.yml` as a blocking step, alongside the five checks already there. **Done** — blocking step added; `paths` extended with `tests/CMakeLists.txt` on both the `pull_request` and `push` triggers (`scripts/check_*.py` was already covered).
- [x] 4.5 Add the check to the workflow's header comment in the same voice as the others: what it protects, and that it exists because guidance alone did not hold — the tree contained an undetected violation (`shotanalysis.cpp`) when it was written. **Done.**

## 5. Documentation

- [x] 5.1 Correct the cost model in `docs/CLAUDE_MD/TESTING.md` "Adding New Tests": the measured five-way split, link at 3%, and that relocating test code between targets does not reduce the cost of compiling it. **Done.**
- [x] 5.2 Correct the same framing in `CLAUDE.md`, which repeats it. **Done.**
- [x] 5.3 Add the value bar to `TESTING.md`: state the defect shape a new test catches that the suite does not; "more of the same shape" is argued as insurance. **Done.**
- [x] 5.4 Add the one-invariant-one-test rule to `TESTING.md`, with the consolidation instruction for the two-fixtures case. **Done.**
- [x] 5.5 Add the narrow-library rule to `TESTING.md`, with the incremental table and the reason `decenza_testlib` is the wrong place. **Done.**
- [x] 5.6 Update the numbers in 5.1–5.5 to the clean-rebuild figures. They were written against the earlier mixed-load read (4,905 s / 2,025 s / 4.6% link) and **Done** — both files now carry 4,846 s / 1,956 s / 3.0% link, the corrected five-way split, and the measured 77.0 s → 12.2 s incremental.
- [x] 5.7 Document the new check in `TESTING.md` next to the rule it enforces, **Done** — plus the three editing rules the cleanup discovered: keep `add_library()` literal (a `foreach()` is invisible to a text check), do not hoist a moc'd header unless every user links the library, and a generated qrc goes in an OBJECT library.

## 6. Verification

- [x] 6.1 Run the full suite through `mcp__qtcreator__run_tests` (scope `all`) and confirm green, with no new warnings. The restructuring changes link topology, so a target that silently depended on a duplicate compile will surface here. **Done — 110/110 green, no warnings, run after each restructuring step.** One intermittent `tst_coffeebags` failure was seen under parallel load (`failOnWarning` firing on an async `QTRY_VERIFY`); it passed in isolation and on both subsequent full runs. It recurred on the final run — **twice in five full runs, always the same function** — so it is a genuine flake, not a one-off. Mechanism found: it fails via `failOnWarning`, not an assertion. `settings_dye.cpp:56` warns "active bag N not found - clearing selection" when `bagReady` arrives with an empty map, which under load happens because the async bag read races the test's raw-connection seed. Pre-existing and unrelated to this change (which touched no test source), so root-causing it is out of scope — but it is now documented in `TESTING.md` alongside the two other tests covered by `--repeat until-pass:3`, with the mechanism recorded so whoever fixes it starts ahead.
- [x] 6.2 Publish the before/after clean-rebuild numbers. **Done — and the honest answer is a RANGE, not a figure.** Duplicates 46 → 0 and qrc 54 s → 28 s are counts and are certain. The wall-clock totals are not: `tests/` fell 19.8% but the untouched app arm fell 14.9%, and that "control" is contaminated — removing 46 parallel compiles reduces contention, so remaining app edges got faster too. Control-adjusted gives ≈96 s; direct accounting of removed work gives 241 s. **The saving is between 96 s and 241 s and wall-clock cannot separate them.** The proposal's −237 s claim is at the top of that range and should not be quoted as measured. Full reasoning in `baseline.md`.
- [x] 6.3 Revert-if-no-saving check. **Not triggered — the saving materialised.** Duplicate compilation went to zero, which is the load-independent claim the restructuring rests on, and the incremental case improved 6×. Keeping sections 2 and 3.
- [x] 6.4 Note the September test-lines-per-source-line check as a follow-up for the maintainer — it cannot be verified in this change, and the proposal states it as a bet. **Command and July figures are in `baseline.md` under 'Growth ratio — the September check'.**
- [x] 6.5 Wiki manual — **not applicable**, no user-visible surface changes. Recorded rather than skipped, per the CLAUDE.md rule.
- [x] 6.6 Archive with `openspec archive cut-test-build-cost` as the final commit on the branch, before merge.
