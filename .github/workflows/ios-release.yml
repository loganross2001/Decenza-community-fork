name: iOS App Store Build

on:
  # Manual trigger from GitHub UI or API
  workflow_dispatch:
    inputs:
      upload_to_appstore:
        description: 'Upload to App Store Connect'
        required: true
        default: 'false'
        type: boolean
      use_cache:
        description: 'Restore/save the compiler cache (nightly builds pass false for a cold build)'
        required: false
        default: true
        type: boolean

  # Trigger on version tags (pre-release and release)
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  actions: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-ios:
    runs-on: macos-26

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0  # Full history for version tags

      - name: Bump version code
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        id: bump
        run: |
          CURRENT=$(cat versioncode.txt | tr -d '[:space:]')
          NEXT=$((CURRENT + 1))
          echo "$NEXT" > versioncode.txt
          echo "build_code=$NEXT" >> $GITHUB_OUTPUT
          echo "Version code bumped: $CURRENT -> $NEXT"

      - name: Pin Xcode to 26.4.1 (matches developer's local libc++ ABI)
        run: |
          # macos-26's default Xcode (26.2) ships a libc++ revision that
          # doesn't include the [abi:ue170006] unstable-ABI symbols Qt
          # 6.11.1's prebuilt iOS binaries reference (qtshadertools,
          # libQt6BundledSpirv_Cross). Result: link fails with
          #   Undefined symbols: std::length_error::length_error[abi:ue170006]
          #   ... vtable for spirv_cross::CompilerError ...
          #
          # Xcode 26.4.1 (Apple clang 21.0.0 build 2100.0.123.102) ships the
          # matching libc++ revision and is the version the developer builds
          # with locally on macOS 26.4 — same Qt 6.11.1 binaries link cleanly
          # there. The macos-26 runner image has 26.4.1 installed alongside
          # the default 26.2 with full iOS platform support (iOS 26.4 SDK +
          # simulator runtimes per the runner readme).
          #
          # The deployment-target shim installed below is still needed:
          # 26.4.1's clang carries the same strict env-conflict check from
          # 26.1+, and the runner image still pre-exports MACOSX/IPHONESIMU-
          # LATOR/MACCATALYST values. Pin + shim are complementary, not
          # redundant.
          sudo xcode-select -s /Applications/Xcode_26.4.1.app/Contents/Developer
          xcodebuild -version
          xcrun --sdk iphoneos clang --version | head -3
          echo "DEVELOPER_DIR=/Applications/Xcode_26.4.1.app/Contents/Developer" >> $GITHUB_ENV

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.11.1'
          target: 'ios'
          arch: 'ios'
          modules: 'qtquick3d qtshadertools qtgraphs qtconnectivity qtmultimedia qtpositioning qtspeech qtwebsockets qtimageformats qtcanvaspainter'
          # Qt-for-iOS caching is intentionally disabled. The iOS Qt cache is too
          # large to coexist with the other platforms under GitHub's 10 GB per-repo
          # cache cap: even after stale-ccache pruning (now prune-caches.yml)
          # trimmed the duplicate ccache piles to ~7.3 GB, re-enabling iOS Qt caching
          # was MEASURED at 3.96 GB for the single qt-ios cache entry (it barely
          # compresses), pushing the store to 10.53 GB — over the cap and back into
          # LRU-eviction thrash. See the experiment in PR #1383 (re-enable) and its
          # revert. iOS is our least cache-critical build, so it pays the ~5 min cold
          # Qt-install cost to keep windows/macos/linux/android Qt caches resident.
          # Do NOT re-enable without first shrinking the iOS Qt cache (e.g. trimming
          # unused modules) so the total stays comfortably under 10 GB.
          cache: false

      - name: Set up ccache
        uses: ./.github/actions/setup-compiler-cache
        with:
          # Key includes a shim-revision tag (s2 = "shim revision 2: also
          # unsets IPHONEOS_DEPLOYMENT_TARGET"). Bump this whenever the
          # shim's env-scrub list changes — the shim's behavior affects
          # compile output, but ccache's compiler_check (even with
          # %compiler% -v) sees the same banner from clang.real regardless
          # of which env vars the shim drops, so a key bump is the only
          # reliable way to invalidate.
          key: 'ios-arm64-xcode26.4.1-s2'
          use_cache: ${{ inputs.use_cache }}

      - name: Configure ccache for CI
        # Run %compiler% -v so the hash captures the real compiler the shim
        # execs (clang.real), not just the shim shell script. This catches
        # underlying Xcode upgrades automatically. (Shim-script changes
        # still need a manual cache-key bump above — see comment there.)
        run: ccache --set-config=compiler_check='%compiler% -v'

      - name: Replace Xcode clang/clang++ with env-scrubbing shims
        run: |
          # The shim sits on the *active* Xcode (26.4.1, set by the pin step
          # above) and intercepts ALL clang invocations — both xcodebuild's
          # native compile path and the AUTOMOC PhaseScriptExecution that
          # calls clang directly without going through CMAKE_XCODE_ATTRIBUTE
          # _CC (which is why a wrapper-only approach can't catch it).
          #
          # Unset ALL platform DEPLOYMENT_TARGET env vars including
          # IPHONEOS_DEPLOYMENT_TARGET. Target/SDK come in via -target /
          # -isysroot flags on the actual command line, so iOS is still
          # correctly identified. With IPHONEOS preserved in the env,
          # clang++'s implicit libc++ resolution at link time goes wrong
          # and the link fails with undefined std::* symbols.
          #
          # Long-term durable fix is upstream: CMake's AUTOMOC predefs
          # script should pass -target explicitly so it doesn't depend on
          # env-var fallback. Tracked as #925.
          set -e
          XCODE_BIN="$(xcode-select -p)/Toolchains/XcodeDefault.xctoolchain/usr/bin"
          for tool in clang clang++; do
            if [ -f "$XCODE_BIN/$tool" ] && [ ! -f "$XCODE_BIN/$tool.real" ]; then
              sudo mv "$XCODE_BIN/$tool" "$XCODE_BIN/$tool.real"
              sudo tee "$XCODE_BIN/$tool" >/dev/null <<EOF
          #!/bin/sh
          unset MACOSX_DEPLOYMENT_TARGET IPHONEOS_DEPLOYMENT_TARGET IPHONESIMULATOR_DEPLOYMENT_TARGET MACCATALYST_DEPLOYMENT_TARGET TVOS_DEPLOYMENT_TARGET WATCHOS_DEPLOYMENT_TARGET XROS_DEPLOYMENT_TARGET DRIVERKIT_DEPLOYMENT_TARGET
          exec "$XCODE_BIN/$tool.real" "\$@"
          EOF
              sudo chmod +x "$XCODE_BIN/$tool"
            fi
          done

      - name: Create ccache compiler wrappers
        run: |
          # Standard ccache plumbing. The Xcode toolchain binaries are already
          # replaced with env-scrubbing shims (step above), so the call chain
          # at compile time is:
          #   xcodebuild → launch-c → ccache → /…/clang (shim) → clang.real
          # The shim handles env scrub; the wrapper only adds ccache.
          WRAPPER_DIR="$RUNNER_TEMP/ccache-wrappers"
          mkdir -p "$WRAPPER_DIR"
          CLANG=$(xcrun -f clang)
          CLANGPP=$(xcrun -f clang++)
          printf '#!/bin/sh\nif [ "$1" = "%s" ]; then shift; fi\nexec ccache "%s" "$@"\n' "$CLANG" "$CLANG" > "$WRAPPER_DIR/launch-c"
          printf '#!/bin/sh\nif [ "$1" = "%s" ]; then shift; fi\nexec ccache "%s" "$@"\n' "$CLANGPP" "$CLANGPP" > "$WRAPPER_DIR/launch-cxx"
          chmod +x "$WRAPPER_DIR/launch-c" "$WRAPPER_DIR/launch-cxx"
          echo "CCACHE_LAUNCH_C=$WRAPPER_DIR/launch-c" >> $GITHUB_ENV
          echo "CCACHE_LAUNCH_CXX=$WRAPPER_DIR/launch-cxx" >> $GITHUB_ENV

      - name: Configure CMake
        run: |
          mkdir -p build/ios
          cd build/ios
          # Wrap only CC/CXX with ccache (not LD/LDPLUSPLUS): ccache
          # doesn't cache link products, and pointing LD at launch-c forces
          # the C++ link through the `clang` driver instead of `clang++`,
          # which drops the implicit -lc++ and breaks the link.
          #
          # Once CC/CXX point at a non-Apple wrapper, Xcode also stops
          # auto-adding the C++ runtime libraries to the Ld phase (and
          # drops -ivfsstatcache and other Apple-internal flags). Compen-
          # sate by setting -lc++ -lc++abi in OTHER_LDFLAGS explicitly.
          $QT_ROOT_DIR/bin/qt-cmake ../.. -G Xcode \
            -DENABLE_TSNET=ON \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
            -DCMAKE_XCODE_ATTRIBUTE_CC="$CCACHE_LAUNCH_C" \
            -DCMAKE_XCODE_ATTRIBUTE_CXX="$CCACHE_LAUNCH_CXX" \
            -DCMAKE_XCODE_ATTRIBUTE_OTHER_LDFLAGS="-lc++ -lc++abi"

      - name: Inject widget extension target
        env:
          TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APP_PROVISIONING_PROFILE_NAME: ${{ secrets.PROVISIONING_PROFILE_NAME }}
          WIDGET_PROVISIONING_PROFILE_NAME: ${{ secrets.WIDGET_PROVISIONING_PROFILE_NAME }}
        run: |
          # qt-cmake -G Xcode only emits the single app target and the
          # project is regenerated every run, so add the WidgetKit
          # app-extension target to the freshly-generated project here,
          # after Configure CMake and before any build.
          gem install --no-document xcodeproj
          ruby ios/inject_widget_extension.rb \
            build/ios/Decenza.xcodeproj \
            "$GITHUB_WORKSPACE"

      # Unsigned compile of the app + injected widget extension. Runs BEFORE
      # the signing setup so a manual dispatch can validate that the Qt
      # build, the pbxproj injection, and the Swift/framework linking work
      # WITHOUT requiring any signing secrets. Dispatch-only: tag releases
      # skip straight to the signed archive (the cert step + archive below
      # are unchanged for releases).
      - name: Build (unsigned compile check — dispatch only)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          cd build/ios
          xcodebuild build \
            -project Decenza.xcodeproj \
            -scheme Decenza \
            -destination "generic/platform=iOS" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_ENTITLEMENTS=""

      - name: Install Apple certificate and provisioning profile
        env:
          P12_CERTIFICATE_BASE64: ${{ secrets.P12_CERTIFICATE_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
          PROVISIONING_PROFILE_BASE64: ${{ secrets.PROVISIONING_PROFILE_BASE64 }}
          WIDGET_PROVISIONING_PROFILE_BASE64: ${{ secrets.WIDGET_PROVISIONING_PROFILE_BASE64 }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          # Create variables
          CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
          PROFILE_PATH=$RUNNER_TEMP/build_profile.mobileprovision
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db

          # Decode from base64
          echo -n "$P12_CERTIFICATE_BASE64" | base64 --decode -o $CERTIFICATE_PATH
          echo -n "$PROVISIONING_PROFILE_BASE64" | base64 --decode -o $PROFILE_PATH

          # Create temporary keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # Import certificate to keychain
          security import $CERTIFICATE_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH

          # Install provisioning profile (must use UUID as filename)
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          PROFILE_UUID=$(/usr/libexec/PlistBuddy -c "Print UUID" /dev/stdin <<< $(/usr/bin/security cms -D -i $PROFILE_PATH))
          cp $PROFILE_PATH ~/Library/MobileDevice/Provisioning\ Profiles/$PROFILE_UUID.mobileprovision
          echo "Installed profile with UUID: $PROFILE_UUID"
          echo "PROFILE_UUID=$PROFILE_UUID" >> $GITHUB_ENV

          # Second profile: the widget app-extension is a separate bundle id
          # (io.github.kulitorum.decenza.widget) and needs its own
          # distribution profile, or CodeSignOnCopy of the .appex fails.
          WIDGET_PROFILE_PATH=$RUNNER_TEMP/widget_profile.mobileprovision
          echo -n "$WIDGET_PROVISIONING_PROFILE_BASE64" | base64 --decode -o $WIDGET_PROFILE_PATH
          WIDGET_PROFILE_UUID=$(/usr/libexec/PlistBuddy -c "Print UUID" /dev/stdin <<< $(/usr/bin/security cms -D -i $WIDGET_PROFILE_PATH))
          cp $WIDGET_PROFILE_PATH ~/Library/MobileDevice/Provisioning\ Profiles/$WIDGET_PROFILE_UUID.mobileprovision
          echo "Installed widget profile with UUID: $WIDGET_PROFILE_UUID"

      - name: Build and Archive
        env:
          TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          cd build/ios

          # Build archive. No global PROVISIONING_PROFILE_SPECIFIER: a
          # command-line specifier applies to ALL targets and would clobber
          # the extension's own profile. Both targets carry their per-target
          # profile in-project (set by inject_widget_extension.rb).
          xcodebuild archive \
            -project Decenza.xcodeproj \
            -scheme Decenza \
            -destination "generic/platform=iOS" \
            -archivePath $RUNNER_TEMP/Decenza.xcarchive \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM=$TEAM_ID \
            CODE_SIGN_IDENTITY="iPhone Distribution"

      - name: Verify widget extension in archive
        run: |
          # The pbxproj injection is the single most failure-prone step; a
          # missing .appex means a green build that silently ships no widget.
          APPEX="$RUNNER_TEMP/Decenza.xcarchive/Products/Applications/Decenza.app/PlugIns/DecenzaWidgetExtension.appex"
          if [ ! -d "$APPEX" ]; then
            echo "::error::Widget extension missing from archive — pbxproj injection did not take"
            exit 1
          fi
          echo "Widget extension present in archive: $APPEX"

      - name: Export IPA
        env:
          KEYCHAIN_PATH: ${{ runner.temp }}/app-signing.keychain-db
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
          WIDGET_PROFILE_NAME: ${{ secrets.WIDGET_PROVISIONING_PROFILE_NAME }}
        run: |
          # Unlock keychain for export
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH

          # Create export options plist using profile UUID from env
          cat > $RUNNER_TEMP/ExportOptions.plist << PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>app-store-connect</string>
              <key>teamID</key>
              <string>${{ secrets.APPLE_TEAM_ID }}</string>
              <key>uploadSymbols</key>
              <true/>
              <key>destination</key>
              <string>export</string>
              <key>signingStyle</key>
              <string>manual</string>
              <key>signingCertificate</key>
              <string>iPhone Distribution</string>
              <key>provisioningProfiles</key>
              <dict>
                  <key>io.github.kulitorum.decenza</key>
                  <string>Decenza App Store</string>
                  <key>io.github.kulitorum.decenza.widget</key>
                  <string>${WIDGET_PROFILE_NAME}</string>
              </dict>
          </dict>
          </plist>
          PLIST

          echo "Export options:"
          cat $RUNNER_TEMP/ExportOptions.plist

          xcodebuild -exportArchive \
            -archivePath $RUNNER_TEMP/Decenza.xcarchive \
            -exportOptionsPlist $RUNNER_TEMP/ExportOptions.plist \
            -exportPath $RUNNER_TEMP/export \
            -allowProvisioningUpdates

      - name: Verify widget extension in IPA
        run: |
          IPA=$(ls "$RUNNER_TEMP"/export/*.ipa 2>/dev/null | head -1)
          if [ -z "$IPA" ] || ! unzip -l "$IPA" | grep -q 'DecenzaWidgetExtension.appex'; then
            echo "::error::Widget extension missing from exported IPA"
            exit 1
          fi
          echo "Widget extension present in IPA: $IPA"

      - name: Upload to App Store Connect
        if: ${{ inputs.upload_to_appstore || startsWith(github.ref, 'refs/tags/v') }}
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_API_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}
        run: |
          # Decode API key
          mkdir -p ~/.appstoreconnect/private_keys
          echo -n "$APP_STORE_CONNECT_API_KEY_BASE64" | base64 --decode > ~/.appstoreconnect/private_keys/AuthKey_${APP_STORE_CONNECT_API_KEY_ID}.p8

          # Upload using altool
          xcrun altool --upload-app \
            -f "$RUNNER_TEMP/export/Decenza.ipa" \
            --type ios \
            --apiKey $APP_STORE_CONNECT_API_KEY_ID \
            --apiIssuer $APP_STORE_CONNECT_API_ISSUER_ID

      - name: Upload IPA as artifact
        uses: actions/upload-artifact@v7
        with:
          name: Decenza-iOS
          path: ${{ runner.temp }}/export/*.ipa
          retention-days: 1
          overwrite: true

      - name: Find dSYMs
        run: |
          echo "# Checking xcarchive dSYMs directory:"
          ls -la $RUNNER_TEMP/Decenza.xcarchive/dSYMs/ 2>/dev/null || echo "(empty or missing)"
          echo ""
          echo "# Checking build output:"
          ls -la build/ios/Release-iphoneos/*.dSYM 2>/dev/null || echo "(not in build output)"
          echo ""
          echo "# Searching workspace for all .dSYM bundles:"
          find $GITHUB_WORKSPACE -name "*.dSYM" -type d 2>/dev/null || echo "(none found in workspace)"
          find $RUNNER_TEMP -name "*.dSYM" -type d 2>/dev/null || echo "(none found in runner temp)"

      - name: Upload dSYMs for crash symbolication
        uses: actions/upload-artifact@v7
        with:
          name: Decenza-iOS-dSYMs
          path: |
            ${{ runner.temp }}/Decenza.xcarchive/dSYMs/
            build/ios/Release-iphoneos/*.dSYM
          retention-days: 90
          overwrite: true
          if-no-files-found: warn

      - name: Cleanup keychain
        if: always()
        run: |
          security delete-keychain $RUNNER_TEMP/app-signing.keychain-db || true
