## 1. Reconcile the plan with the measurement

The audit ran during design and inverted several of the proposal's assumptions: 52 of 63 common profiles are already machine-equivalent, all 11 divergences resolve in Decenza's favour, and no Decenza built-in needs correcting. These tasks bring the written artifacts in line before any code is written.

- [x] 1.1 Update `specs/builtin-profile-sync/spec.md`: replace the A-Flow/D-Flow exclusion requirement with the classify-by-cause rule from design D4, and drop the STUB banner. The scenario "An A-Flow or D-Flow difference against reaprime is not treated as a defect" becomes "A divergence with a known upstream cause is classified, not queued as a Decenza defect."
- [x] 1.2 Add a spec requirement for the machine-observable equivalence relation (design D1) — this is the definition the tool, the gate and the audit all depend on, and nothing owns it yet.
- [x] 1.3 Strike the now-void scope from `proposal.md` with a one-line reason each: content/dedup fixes to built-ins, the retire/refresh user migration, and the Visualizer canonical-JSON sourcing dependency. Leave the parity-gated user-profile conversion in place.
- [x] 1.4 Correct the three stale counts under "Why" (~63 / ~14 / ~50) to the measured 63 / 8 / 3, citing the reaprime commit the measurement used.

## 2. Two `profile_sync` defects this work surfaced

Both concern the **de1app** comparison, not reaprime, which is why they survive the trim below.

> **Cut from this change: the `--reaprime` leg in `profile_sync`** (a `--reaprime <dir>` source, a shared normalisation helper, title-joining, cause classification, a source-commit banner). The comparison it would automate has already been run, its answer is recorded in the audit, and the corpus it compares against is now `fix-legacy-profile-ingest`'s responsibility in the reaprime repo. Productionising a three-way tool here buys re-run convenience for a question that is answered. **Cost accepted:** re-running the comparison after reaprime lands their change is a manual step with throwaway scripts, and nobody is reminded to do it.

- [x] 2.1 Convert the "plugin copy wins" precedence at `profile_sync.cpp:289` into a loud assertion when base and plugin copies disagree (design risk 1) — de1app #350 may resolve in a way that inverts it, and a silent precedence rule would pick the stale file. Note the tool cannot currently compare two `.tcl` sources at all: `frameDiffReport()` is `Profile`-vs-`Profile` so it already works, but `De1AppTcl::compareScalars()` is tcl-vs-JSON by signature and needs an overload. This gap is why our own tooling saw the shadowed 6-frame A-Flow copy, discarded it silently, and reported success. Done — `SOURCE CONFLICT` on stderr naming both paths, the scalar and frame diffs, and a count in both streams. Non-gating (exit stays 0): the precedence is a documented decision, not a guess, and #350 resolving either way removes the conflict entirely. Verified: fires on exactly the four shadowed A-Flow profiles.
- [x] 2.2 Resolve the `settings_2c2` divergence in `De1AppTcl::isAdvancedType()`. It returns true for `settings_2c2`, which matches de1app's *frame* handling (`binary.tcl:1048`, `gui.tcl:1586`, `profile.tcl:469` all treat `2c`/`2c2` alike) but **not** its stop-target handling: both runtime switches special-case `settings_2c` alone (`device_scale.tcl:1322`, `de1_de1.tcl:862`, each `settings_2c {...advanced} default {...plain}`). So a `2c2` profile would take our `_advanced` target where de1app takes the plain one. No profile in any of the three corpora is typed `2c2`, so no built-in is affected — but a user profile could be. Decide whether to match de1app's inconsistency or keep ours, and record which and why. **No code change — the divergence is not real.** `::profile::fix_profile_type` treats `settings_2c2` as a legacy alias and rewrites it to `settings_2c`, and de1app applies that into `::settings` on load (before `sync_from_legacy`) and on save. The stop-target switches therefore never observe `2c2`. Recorded as a comment on `isAdvancedType()` so the same misreading is not repeated. No profile in any of the three corpora carries the type.

## 3. The audit document

> **Cut from this change: the reaprime verdict corpus and its regression test** (`tests/data/profiles_reaprime_verdicts.json`, `tests/tst_reaprimeequivalence.cpp`). Two reasons, the second decisive. The drift that matters — *our* built-ins changing — is already gated by the immutable golden corpus at `tests/data/profiles_legacy/`. And the test would encode "these 11 diverge from reaprime" at exactly the moment reaprime is fixing all 11, so it would go red through no fault of ours and the fix would be regenerating the fixture. A test that must be regenerated whenever another project ships is a chore, not a gate. Regression cover for the reaprime corpus belongs in `fix-legacy-profile-ingest`, which has it. **Cost accepted:** no automated signal if reaprime later regresses.

- [x] 3.1 Write the per-profile audit into the change directory: all 63 with verdict and evidence, the three encoding classes with their row counts, and the de1app source type for each of the 11. Written to `audit.md`.
- [x] 3.1a Save the comparison script alongside the audit. Cutting the `--reaprime` leg makes re-running a manual step, and the scripts that produced these numbers are in a session scratchpad that does not survive. A ~60-line Python file next to the audit is what makes "re-run it when reaprime lands" a real option rather than a re-derivation. Saved as `compare_reaprime.py`; re-run instructions in the audit.
- [x] 3.2 Record the two `settings_2c` cases (`Advanced spring lever`, `Cleaning/Forward Flush x5`) as the check on the audit's own conclusion — advanced profiles involve no frame regeneration, so Decenza has no mechanism to be accidentally right there. In `audit.md` under "The check on this audit's own conclusion" — both pass.
- [x] 3.3 Fold the durable conclusion into `docs/CLAUDE_MD/RECIPE_PROFILES.md`: reaprime's built-ins carry de1app's stored `advanced_shot` frames, so a difference against them on a simple profile is evidence about reaprime, not about Decenza. Added to the `settings_2a`/`2b` corollary section.

## 4. Upstream

The reaprime side is no longer authored here. `fix-legacy-profile-ingest` was written into the reaprime clone (proposal, design, specs, 35 tasks) and is tracked separately — it covers the converter fix, the type-resolved stop targets, the A-Flow re-harvest, the beverage-type passthrough and the corpus rebuild. What remains here is handover and the dependency we cannot close ourselves.

> **Cut from this change: "hold the A-Flow rows pending de1app #350."** It assumed we would send reaprime our 9-frame JSON, where a later re-harvest from de1app could silently undo it. `fix-legacy-profile-ingest` instead has them re-harvest from `de1plus/plugins/A_Flow/profiles/` themselves, with a guard that fails loudly when base and plugin copies disagree — correct under either resolution of #350, so nothing needs holding.

- [x] 4.1 Decide issue-first vs PR for approaching `tadelv/reaprime`, and lead with the mechanism rather than a diff — the 75 °C/54 °C brew temperatures in their `Default` are the one-line demonstration. **Done — PR submitted to `tadelv/reaprime`.** The change was implemented rather than proposed, so the issue-first question resolved itself.
- [x] 4.2 Track de1app #350 to resolution and record which way it went, since it decides whether `profile_sync`'s canonical A-Flow source is the plugin or the base directory. **This is also the only divergence neither change can close**: de1app's own users keep brewing 6-frame A-Flow until it is fixed upstream. **Closed as a watch item, not work.** Nothing here depends on the outcome: `profile_sync` now reports a `SOURCE CONFLICT` naming both paths when the base and plugin copies disagree (task 2.1), so either resolution of #350 surfaces loudly instead of silently selecting a stale file. de1app's own users keep brewing 6-frame A-Flow until Decent/Jan3kJ fix it — outside both changes.
- [x] 4.3 Correct the record on de1app #350's second finding if it comes up: the float round-trip noise cited there as evidence that the released `best_practice_light.tcl` was not written by de1app's save path does **not** support that claim. de1app's own repo profiles carry the same noise — `Cleaning_forward_flush_x5.tcl` ships `pressure 9.999999999999993` and `-5.717648576819556e-15`. The `water_temperature` key (absent from `profile_vars`, so `save_profile` cannot emit it) still stands and is the argument to lead with. **Closed — the correction is recorded, which was the point.** `audit.md` carries it under "What remains open": de1app's own repo profiles show the same float noise (`Cleaning_forward_flush_x5.tcl` ships `pressure 9.999999999999993`), so that evidence does not support the claim, and `water_temperature` — absent from `profile_vars`, so `save_profile` cannot emit it — is the argument that does. Available if anyone replies to #350; no comment posted, since it is Jeff's issue to answer on.

## 5. User-profile encoding upgrade, on load

Sequenced last per design D6. This is the only work in the change that touches a user's disk.

> **Rescoped from a one-time startup pass to upgrade-on-load.** A one-time pass cannot be correct: a user drops profiles into the directory whenever they like — sideloaded, restored from a backup, synced from another device — so any pass that marks itself complete leaves every later arrival unconverted. Upgrading when a profile is actually loaded covers files that arrive at any time, needs no completion flag, and touches nothing the user never uses. **Task 5.3 (the completion flag) is deleted by this rescope, not deferred.**

- [x] 5.1 Make `Profile::saveToFile` atomic (`QSaveFile`: write to temp, `commit()` renames). It currently opens `WriteOnly`, truncating in place — tolerable for git-tracked built-ins, not for rewriting a user's file purely to reformat it, where a crash or full disk mid-write destroys a profile that was fine. Fixing it here rather than only at the call site protects every writer. Done — `QSaveFile` + `commit()`, with the failure path documented (a missing `commit()` is a silent no-op that reads as success).
- [x] 5.2 Add the upgrade to `ProfileManager::loadProfile`'s 4-tier resolve, scoped to the **writable** tiers: ProfileStorage (SAF), `userProfilesPath()`, `downloadedProfilesPath()`. Tier 4 (`:/profiles/`) is a read-only Qt resource and must be skipped. Deliberately **not** hooked in `Profile::loadFromFile` — 28 call sites outside `profile.cpp` read from resources, temp files and staging dirs, none of which should be rewritten. Done — `ProfileManager::upgradeStoredEncoding()`, called from `loadProfile` on the Storage/LocalFile tiers only, via an explicit `Origin` enum rather than inferring from the path.
- [x] 5.3 Write only when `Profile::jsonParityErrors(original, converted)` is empty. Audit before writing, never after — the `--rewrite-format` path documents why: an earlier revision wrote first and audited the file it had just clobbered, so by the time "DATA LOSS" appeared the original only existed in git. Done — parity checked against the bytes actually on disk, before any write.
- [x] 5.4 Skip silently when the stored object already equals the canonical one, so a loaded profile is not rewritten on every activation. Compare the parsed objects, not the bytes — formatting differences that survive a round trip would otherwise churn the file and its mtime forever. Done — compares parsed objects; verified by `loadDoesNotRewriteAnAlreadyCanonicalProfile`.
- [x] 5.5 Log a skip once per profile when parity fails, naming the profile and the keys at risk. A profile that cannot be converted losslessly is a signal about the serializer, not noise to swallow. Done — `qWarning` naming the profile and the drifting keys.
- [x] 5.6 Route the SAF tier through `ProfileStorage::writeProfile()`, not `QFile` — on Android the profile directory may be a SAF tree with no usable filesystem path. Done — `filePath.isEmpty()` selects the `ProfileStorage::writeProfile` path.
- [x] 5.7 Test: a legacy-encoded profile is upgraded on load; a deliberately lossy fixture is left untouched with a log line; an already-canonical profile is not rewritten; a built-in is never written. Done — 4 tests in `tst_profilemanager.cpp`: upgrade, no-rewrite-when-canonical, built-in untouched, lossy left byte-for-byte with an asserted warning.
- [x] 5.8 Verify a converted profile survives `DatabaseBackupManager`'s `copyDirectory()` into a backup and restores readable — the reason the conversion exists. Done — `upgradedProfileSurvivesABackupRoundTrip` copies the converted file and asserts `reaprimeReadabilityErrors` is empty on the far side.

## 6. Close out

- [x] 6.1 Update the wiki manual if the conversion surfaces anything user-visible; note explicitly if it does not, so the omission is a decision rather than an oversight. **No manual change needed — recorded as a decision.** The wiki manual does not enumerate built-in profiles, so the seven additions need no entry there; they are covered in the release notes instead. The encoding upgrade is deliberately invisible (same profile, same shot, different bytes on disk) and has nothing a user acts on.
- [x] 6.2 Run the full suite through Qt Creator MCP (scope `all`) and confirm zero warnings — the pre-PR gate, since there is no PR CI. Done — 98 passed, 0 failed, 0 skipped, and `tests_with_warnings` empty.
- [x] 6.3 `openspec archive sync-builtin-profiles` as the last commit on the branch, before merge. Done — archived via `openspec archive` as the final commit on the branch.
