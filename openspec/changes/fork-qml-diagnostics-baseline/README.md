# fork-qml-diagnostics-baseline

Give the fork its own qmllint baseline so the QML diagnostics gate can pass on a clean fork build — today it carries upstream's baseline, which knows nothing about the fork's QML.
