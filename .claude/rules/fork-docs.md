Fork design, request, and resume documents live in `docs/barista/`, never at the repository root.
The repository root holds only upstream-authored Markdown (`AGENTS.md`, `CLAUDE.md`, `PRIVACY.md`, `README.md`).
The fork's capability reuse index is `.fork/INDEX.tsv`; the fork contract is `.fork/FORK.md`.
