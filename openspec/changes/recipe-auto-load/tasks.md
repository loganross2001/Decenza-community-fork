## 1. Settings — recipe-side auto-load key

- [x] 1.1 Add `autoLoadRecipeId` (`int`, sentinel `-1`) `Q_PROPERTY` + getter/setter/NOTIFY to `SettingsDye` (`src/core/settings_dye.h/.cpp`), matching the existing `activeRecipeId`/`activeBagId` shape (`src/core/settings_dye.cpp:604-623`).
- [x] 1.2 Wire mutual exclusion in `Settings` (the façade that owns both `SettingsApp` and `SettingsDye`), not inside either domain class: connect `SettingsApp::autoLoadProfileFilenameChanged` to a slot that clears `dye()->autoLoadRecipeId` to `-1` when the filename is non-empty, and `SettingsDye::autoLoadRecipeIdChanged` to a slot that clears `app()->autoLoadProfileFilename` to `""` when the id is non-negative. Guard against infinite re-entry (each setter already no-ops when the value is unchanged).
- [x] 1.3 Confirm settings backup/restore treatment of `dye/autoLoadRecipeId`. **Correction from the original plan**: it does NOT round-trip — `settingsserializer.cpp:146-153` deliberately excludes `dye/activeBagId`, `dye/activeEquipmentId`, and `dye/activeRecipeId` as device-local DB row ids; `autoLoadRecipeId` is the same kind of value and now documented alongside them. Only the shared `autoLoadRevertMinutes` (already exported under `profile.autoLoadRevertMinutes`) round-trips. Spec corrected to match (see `specs/recipe-auto-load/spec.md`).

## 2. Recipe activation entry point

- [x] 2.1 Add `Q_INVOKABLE void loadAutoLoadRecipeIfNeeded()` to `MainController` (`src/controllers/maincontroller.h/.cpp`): no-op if `autoLoadRecipeId == -1`; look up the recipe via `m_recipeStorage`, clearing + emitting a stale-cleared signal if it doesn't exist or is archived; no-op if it equals `Settings.dye.activeRecipeId`; otherwise call `activateRecipe(autoLoadRecipeId)`. **Implementation note**: `RecipeStorage` has no synchronous single-row accessor (all reads are async via `requestRecipe`/`recipeReady`), so the existence/archived check is async: a permanent `recipeReady` connection in `setupRecipeConnections()`, filtered on a new `m_pendingAutoLoadRecipeId` member, decides clear-vs-activate.
- [x] 2.2 Add an `autoLoadRecipeStaleCleared()` signal on `MainController`, mirroring `ProfileManager::autoLoadStaleCleared()` (`src/controllers/profilemanager.h:341`).
- [x] 2.3 **Revised approach**: rather than touching `RecipeStorage` (which stays settings-ignorant per the existing layering), hooked the same generic `recipeUpdated`/`recipeDeleted` signals `MainController` already uses for the active-recipe cache refresh (`setupRecipeConnections()`, mirroring the `recipeId != m_settings->dye()->activeRecipeId()` pattern at `maincontroller.cpp:1021`) — any successful update to the current auto-load recipe re-triggers the same async archived-check via `m_pendingAutoLoadRecipeId`/`requestRecipe`; a successful delete clears directly (no row left to re-read).

## 3. Trigger wiring (`qml/main.qml`)

- [x] 3.1 Add `MainController.loadAutoLoadRecipeIfNeeded()` alongside the existing `ProfileManager.loadAutoLoadProfileIfNeeded()` call at app startup (`:931`).
- [x] 3.2 Add the same call alongside the DE1 `Sleep → Idle` wake handler (`:505-506`), reusing the existing reconnect-guard tracking (`autoLoadPreviousDe1State`) unchanged.
- [x] 3.3 Add the same call inside the idle-countdown `Timer`'s `onTriggered` (`:452-453`).
- [x] 3.4 Widen `autoLoadCountdownReload()` (`:460-470`)'s early-out check so the countdown is disabled only when **both** `Settings.app.autoLoadProfileFilename === ""` **and** `Settings.dye.autoLoadRecipeId === -1` (currently checks only the filename).
- [x] 3.5 Add a `Connections { target: Settings.dye; function onAutoLoadRecipeIdChanged() { root.autoLoadResetCountdown() } }` block alongside the existing `Connections { target: Settings.app; ... }` block (`:476-480`) so changing the recipe target resets the countdown the same way changing the profile target already does.
- [x] 3.6 Wire a toast for `MainController.autoLoadRecipeStaleCleared` next to the existing profile stale-clear toast wiring (`:4172`), with recipe-appropriate wording ("Auto-load recipe is no longer available"). Implemented by giving the shared `autoLoadStaleToast` a `message` property swapped per-trigger, rather than a second toast rectangle.

## 4. Recipe card UI (`qml/pages/RecipesPage.qml`)

- [x] 4.1 Add a `StyledIconButton` (36×36, `icon.source: "qrc:/icons/pin.svg"`) as the first child of the card's `footer: Flow` (`:466`), before the existing Edit button (`:470-478`), `visible: !card.archivedCard`.
- [x] 4.2 Tint the icon `Theme.primaryColor` when `card.recipe.id === Settings.dye.autoLoadRecipeId`, default/outline color otherwise. **Implementation note**: used `StyledIconButton`'s own built-in `active`/`activeColor`/`inactiveColor` toggle-state API (it already wraps a `MultiEffect` colorization internally) rather than adding a separate `MultiEffect` block as `ProfileSelectorPage.qml` does for its plain pin `Image` — simpler given the button component already supports this.
- [x] 4.3 `onClicked`: if not currently the auto-load target, set `Settings.dye.autoLoadRecipeId = card.recipe.id`; if it is, clear to `-1`. No confirmation dialog either way.
- [x] 4.4 Show a toast on toggle ("Auto-load set for <recipe name>" / "Auto-load disabled"), including the displacement case (profile or another recipe was previously auto-loaded) per the confirmed "switch silently + toast" behavior. Reused (and renamed) the page's existing generic toast mechanism — `recipesPage.showFailureToast()` → `showToast()` — instead of adding a second toast surface.
- [x] 4.5 Add `Accessible.name` reflecting state ("Set auto-load" / "Disable auto-load"), per `ACCESSIBILITY.md` conventions. Audited the rest of the footer (Edit/Clone/Archive/Delete/Restore) — all already have `accessibleName` set via `StyledIconButton`/`AccessibleButton`'s own built-in accessibility wiring; no pre-existing gaps found in this file's footer.
- [x] 4.6 Add an on-card status row, `visible: card.recipe.id === Settings.dye.autoLoadRecipeId`, as another item in the same `footer: Flow` (`RecipesPage.qml:466`): a small pin icon, an "Auto-load" text label, and a `ValueInput` bound to `Settings.app.autoLoadRevertMinutes` (0..60 range, live `onValueModified` write, "off" display at 0 — same config as `ProfileSelectorPage.qml`'s strip, new recipes-namespaced translation keys rather than reusing the profile page's).
- [x] 4.7 Do not repeat the recipe name or add a clear/× button in this row — the toggle button from tasks 4.1-4.4 already clears auto-load on a second tap.
- [x] 4.8 Wire the status row's stepper's live edit to reset the idle-countdown timer. **No extra call needed**: `main.qml`'s existing `Connections { target: Settings.app; function onAutoLoadRevertMinutesChanged() { root.autoLoadResetCountdown() } }` (task 3.5's block) already reacts to the property-changed signal regardless of which UI surface wrote it, so writing `Settings.app.autoLoadRevertMinutes = newValue` from the card's stepper is sufficient.
- [x] 4.9 Give the stepper an accessible name ("Auto-load revert after, minutes") consistent with `ACCESSIBILITY.md` conventions.

## 5. MCP tools

- [x] 5.1 Add `recipe_get_auto_load` (read access level). **Relocated to `src/mcp/mcptools_write.cpp`** (not `mcptools_recipes.cpp` — see the correction below): returns `{ recipeId, name, revertMinutes }`, with `recipeId: null` when unconfigured or when the pinned id no longer resolves (treated as a stale snapshot, not an error — the next trigger clears it).
- [x] 5.2 Add `recipe_set_auto_load` (settings access level) to `src/mcp/mcptools_write.cpp`: validates `recipeId` present, resolves to an existing non-archived recipe on a background thread (`RecipeStorage::loadRecipeStatic`, matching `recipe_get`'s pattern), then on the GUI thread sets `Settings.dye.autoLoadRecipeId` (triggers the cross-clear from task 1.2) and optionally `Settings.app.autoLoadRevertMinutes`. Error cases: missing `recipeId`, not found, archived — matching `profiles_set_auto_load`'s response shape.
- [x] 5.3 Add `recipe_clear_auto_load` (settings access level) to `src/mcp/mcptools_write.cpp`: clears `autoLoadRecipeId` to `-1` only, leaving `autoLoadRevertMinutes` untouched.
- [x] **Correction (discovered while writing task 6.5's tests)**: all three were originally placed in `mcptools_recipes.cpp` (next to `recipe_get`/`recipe_archive`, per the plan below). Relocated to `mcptools_write.cpp` instead — `mcptools_recipes.cpp`'s `recipe_activate`/`recipe_archive` call real `MainController` methods directly, so linking that file at all (for ANY tool, even ones that don't touch `MainController`) requires resolving those symbols, pulling in `MainController`'s full ~20-subsystem closure (see task 6.3's finding). This is why **no** recipe MCP tool has ever had test coverage. `mcptools_write.cpp` already takes `ShotHistoryStorage*`/`Settings*` (all these three tools need), has no `MainController` dependency, and is already tested via `tst_mcptools_write.cpp` — confirmed by a clean `--target all` build after the move.
- [x] 5.4 Update the tool descriptions on `profiles_set_auto_load` and `profiles_clear_auto_load` (`src/mcp/mcptools_write.cpp`) to document the cross-clear behavior with the new recipe tools.
- [x] 5.5 No registry/stub changes needed — `registerRecipeTools`'s signature is unchanged (new tools registered inside the existing function body), and no test file enumerates tool names or asserts a total tool count. The `project_mcp_register_stubs_in_tests` risk applies only to signature changes on `register*Tools`, which this isn't. Verified via a full `--target all` build (task 6.6 will re-verify after the test additions).

## 6. Tests

- [x] 6.1 `tests/tst_settings.cpp`: default `-1` (`autoLoadRecipeIdDefaultIsMinusOne`), set/get/signal round-trip (`autoLoadRecipeIdRoundTrip`), clamping is unaffected (shared with profile, already covered by the existing `autoLoadRevertMinutesClamped`), export/import behavior (`autoLoadRecipeIdNotExportedAsDeviceLocalId` — see 6.1's correction below).
- [x] 6.2 `tests/tst_settings.cpp`: mutual exclusion — `autoLoadMutualExclusion_recipeClearsProfile`, `autoLoadMutualExclusion_profileClearsRecipe`, `autoLoadMutualExclusion_clearingOneDoesNotSpuriouslyTouchOther` (asserts no spurious signal on the untouched side).
- [ ] ~~6.3 `tests/tst_maincontroller.cpp`: ...~~ **Not attempted — scoping finding, flagging for a decision rather than building it.** `MainController`'s constructor unconditionally instantiates ~20 heavyweight subsystems (`ShotServer`, `MqttClient`, `VisualizerUploader`/`Importer`, `BeanBaseClient`, `FirmwareUpdater`, `DataMigrationClient`, `LocationProvider`, `ShotReporter`, `UpdateChecker`, etc. — see `maincontroller.cpp`'s constructor). No test in this suite constructs a real `MainController` today (grepped all of `tests/*.cpp` — zero hits); every reference is a comment or string literal. `ProfileManager` and `RecipeStorage` are independently testable (and already have `tst_profilemanager.cpp` / `tst_recipestorage.cpp`) precisely because they don't pull in this closure. Building a standalone CMake test target for `MainController` would mean linking most of the app's controller graph for one feature's tests — a large, fragile, one-off undertaking with no precedent, not a proportionate cost for covering `loadAutoLoadRecipeIfNeeded()`. **What actually covers this method today**: manual QA in the running app (see conversation) exercised the no-op/already-active path, the mutual-exclusion cross-clear (both directions), and the toggle/toast/status-row UI — all passed. The stale-clear (recipe missing/archived) and successful-activation-call-through paths were NOT exercised by either automated tests or manual QA. **Left for the user to decide**: accept this gap, or treat "make MainController's auto-load logic unit-testable in isolation" (e.g. extracting it into a small testable helper) as separate follow-up work.
- [ ] ~~6.4 Recipe storage tests: archiving/deleting the current auto-load recipe clears `autoLoadRecipeId` immediately.~~ **Same blocker as 6.3, not a separate gap.** Per task 2.3's revised implementation, this behavior lives in `MainController`'s `recipeUpdated`/`recipeDeleted` connections (`setupRecipeConnections()`), not in `RecipeStorage` itself — `RecipeStorage` has no knowledge of `autoLoadRecipeId` at all. So this can't be tested at the `RecipeStorage` level in isolation (unlike `activeNameUniqueness` etc., which test real `RecipeStorage` behavior); it needs the same real `MainController` instance task 6.3 couldn't justify building. Folded into the same follow-up (task_43425fb5, spawned this session).
- [x] 6.5 MCP tool tests added in `tests/tst_mcptools_write.cpp` (the tools' relocated home — see 5.1-5.3's correction): `recipeGetAutoLoadUnconfiguredReturnsNull`, `recipeGetAutoLoadConfiguredReturnsNameAndId`, `recipeGetAutoLoadStaleIdReturnsNullNotError`, `recipeSetAutoLoadSuccessSetsSettingsAndClearsProfile` (also proves the cross-clear), `recipeSetAutoLoadMissingRecipeIdIsError`, `recipeSetAutoLoadNotFoundIsError`, `recipeSetAutoLoadArchivedIsError`, `recipeSetAutoLoadOptionalRevertMinutesUpdatesSharedSetting`, `recipeClearAutoLoadSuccessPreservesRevertMinutes`. All 9 pass (`mcp__qtcreator__run_tests`, `tst_mcptools_write`: 1/1). No registry/stub changes needed (same reasoning as 5.5 — no signature change).
- [x] 6.6 Ran the full suite via `mcp__qtcreator__run_tests` (scope `all`): 92 passed, 0 failed, 0 skipped, no warnings.

## 7. Documentation

- [x] 7.1 Updated the GitHub wiki manual (`Kulitorum/Decenza.wiki.git`, `Manual.md`): new "Auto-Load Recipe" section under Recipes (card button, shared revert-timer, mutual exclusion), plus a cross-reference note added to the existing "Auto-Load Profile" section. Committed and pushed (`e14c46f`).
