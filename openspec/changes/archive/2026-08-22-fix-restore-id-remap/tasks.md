## 1. Import result and shot id map

- [x] 1.1 Add an `ImportResult` struct to `shothistorystorage.h` carrying `shotIdMap` (source id → dest id), `destShotsBefore`, `imported`, `skipped`, `failed`, `referencesRemapped`, `referencesCleared`.
- [x] 1.2 Add an `ImportResult*` out-parameter to `ShotHistoryStorage::importDatabaseStatic`, defaulting to `nullptr`; keep the `bool` return.
- [x] 1.3 Populate `shotIdMap` as each source shot is inserted, alongside the existing `packageIdMap` / `bagIdMap` / `recipeIdMap` handling. A skipped duplicate maps the source id to the id of the destination row it matched; a failed row is absent from the map.
- [x] 1.4 Populate the counts and replace the `imported/skipped/failed` summary `qDebug` with one that also states `destShotsBefore`, so merging into an empty destination no longer logs identically to merging into a populated one.
- [x] 1.5 Check `shothistorystorage.cpp` against `COVERED_GLOBS` / `MARKER_ONLY_GLOBS` in `scripts/check_log_markers.py` before touching log lines there, and follow `docs/CLAUDE_MD/LOGGING.md` for tier and marker. Audience picks the tier: a refused restore is user-facing.
- [x] 1.6 Update the three call sites to pass an `ImportResult`: `databasebackupmanager.cpp:872`, `datamigrationclient.cpp:1091`, `shotserver_backup.cpp:1179`.

## 2. Merge-integrity guards

- [x] 2.1 Check the result of the merge-mode de-duplication pre-read (`SELECT uuid FROM shots`, `shothistorystorage.cpp` ~4276). On failure, roll back and return a failure naming the pre-read; do not proceed with an empty set.
- [x] 2.2 Add an independent `SELECT COUNT(*) FROM shots` on the destination in merge mode and record it as `destShotsBefore`.
- [x] 2.3 Abort before the first insert when the pre-read returned zero and the count is non-zero; report both numbers. Merge mode only — replace mode clears the destination first and an empty pre-read is expected there.
- [x] 2.4 Confirm both guards run inside the existing transaction and leave the destination unchanged on abort.

## 3. One conversation importer, one remap

- [x] 3.1 Extract the hand-rolled AI conversation import — duplicated at `databasebackupmanager.cpp:1116-1143`, `datamigrationclient.cpp:618-660` and `shotserver_backup.cpp:1118-1163` (THREE copies, not the two the plan assumed) — into a single shared function. Landing it in one commit with the remap: splitting was dropped, the extraction has no independent value and the endpoint's reordering moves with it.
- [x] 3.2 Delete all three original copies and route each caller through the shared function. Verify they were behaviourally identical first; if they differ, record which behaviour was kept and why.
- [x] 3.2a Backup endpoint only: its conversation block runs BEFORE the shot import, so there is no id map at that point. Stash the conversations array during the zip-entry loop and import it in the existing main-thread continuation, once `success` and the map are known. Keep `reloadConversations()` on the main thread and the `aiConversationsImported` response field correct on both the async and the no-shots-db paths.
- [x] 3.3 Add the shot id map as a parameter of the shared function: rewrite each turn's `shotId` to the destination id, drop the key when the source id is absent from the map, and return the remapped and cleared counts.
- [x] 3.4 Clear every turn's `shotId` when no map is supplied — the conversations-only import path (`importOnlyAIConversations()`, or a user deselecting shots). The ids name a database this device does not have.
- [x] 3.5 Feed the returned counts into `ImportResult.referencesRemapped` / `referencesCleared` so the import summary reports them.
- [x] 3.6 Grep the settings surface for any other stored shot-id reference (`src/core/settings*`, `settingsserializer.cpp`, SAW learning, shot badges). Route any found through the same helper; record in the PR that the sweep ran and what it found, including "nothing else". **Result:** one other key stores shot ids — `migration16/pendingVisualizerSync` (`maincontroller.cpp:709`, `:4896`), an array of `{shotId, visualizerId}`. It does NOT cross an import: `SettingsSerializer::exportToJson` is an explicit per-property allowlist, not an `allKeys()` sweep, so the key is never exported or restored and stays device-local. SAW learning (`saw/*`) stores per-profile/basket keys and lag values, no shot ids. No shot-badge persistence outside `shots.db`. AI conversation turns are the only stored shot-id reference that crosses an import.

## 4. Stale ids stop being acted on

- [x] 4.1 In `AIConversation::loadFromStorage`, resolve the distinct turn `shotId`s against the database once and treat unresolvable ones as absent in memory. Do not write back.
- [x] 4.2 Make `shotIdForTurn` return `0` and `recentAssistantTurns` skip a turn whose id did not resolve, matching the existing treatment of a turn that never carried an id.
- [x] 4.3 Measure the added read on a realistic database and on a multiple of one, and record median and worst case in a comment at the call site — evidence, not the conclusion. **Done against the real tablet database** (1061 shots / 18.1 MB, pulled over `/api/backup/shots`) and a synthetic 4x copy (4244 shots / 45.1 MB), 200 runs per point. Median 0.018-0.045 ms, worst 1.02 ms on a cold cache; flat against 4x the rows. Query plan is `SEARCH shots USING INTEGER PRIMARY KEY (rowid=?)`. Numbers and the caveat (measured on the Mac against the tablet's file, not on the tablet's own hardware) are in the comment at the call site. No hoist needed.
- [x] 4.4 Surface the `requestUpdateShotMetadata` success flag to `AIManager` so a write to a missing shot reports failure to its caller instead of only warning. What the app does about it (retry, re-ask, tell the user) is out of scope — this stops it vanishing.

## 5. User-facing failure

- [x] 5.1 Add a restore failure message naming the inconsistency and stating that nothing was changed. Follow the existing `(translation key, English fallback)` pair convention in `databasebackupmanager.cpp` — worker thread accumulates, `joinErrors` translates on the main thread.
- [x] 5.2 Make the same refusal reach the LAN migration and backup-endpoint surfaces; verify none of the three reports a refused import as a completed restore.
- [x] 5.3 Add the translation keys with English fallbacks.

## 6. Tests

- [x] 6.1 Add slots to `tests/tst_shotimportdedupe.cpp` (do not create a new test file). Written: failed pre-read aborts without writing (real malformed destination — a shots table with no uuid column); agreeing counts proceed and skip duplicates; replace mode is not subject to the check; genuinely empty destination imports normally; and `existingShotIds` answers exactly and refuses to answer when it cannot look.

  **The second guard was REPLACED during review rather than kept untested.** It read `destShotsBefore > 0 && existingByUuid.isEmpty()`, and `shots.uuid` is `TEXT UNIQUE NOT NULL` (`createTables`) with no migration adding it nullable — so a complete read yields exactly `COUNT(*)` distinct keys and the two conditions are **mutually exclusive by schema constraint**. No honest destination reaches it. It is now an equality check, `existingByUuid.size() != destShotsBefore`, which covers that case as a subset and additionally catches a **truncated** read: `next()` returning false is how end-of-rows and a mid-scan error look the same, and a mid-scan `SQLITE_BUSY` during a restore is a mechanism that can actually occur. Still not directly testable without injection, and still not faked — but it now guards something reachable rather than something the schema forbids.

  **Correcting the earlier justification here**: this task previously said "the field evidence for that state is real". It is not. `design.md`'s own conclusion is that the destination's `shots` table was dropped and recreated, so it was genuinely empty and `COUNT(*)` would have been 0 — the old guard would not have fired on 2026-08-21 either. GUARD 1 is what covers the field incident, and GUARD 1 is tested.

- [x] 6.2 Add slots covering the id map: an imported shot's map entry is its destination id; a skipped duplicate maps to the matched destination row; a failed row is absent from the map.
- [x] 6.3 Add slots to `tests/tst_aimanager.cpp`: turns are rewritten to destination ids; a turn whose shot did not come across loses its `shotId` and is skipped by `recentAssistantTurns`; a conversations-only import clears every id; a metadata write to a missing shot reports failure.
- [x] 6.4 Add a regression test for the reported defect: import a source whose shot ids exceed anything in the destination, then assert no stored reference names a source id — the shape that would later resolve to a real but unrelated shot.
- [x] 6.5 Before keeping each test, break the code it covers and watch it go red. Drop any that cannot fail.

  **This record was wrong when first written, and the review caught it.** It claimed a third mutation — "making `loadFromStorage` skip the resolve pass: the `recentAssistantTurns` slot went red". That was not reproducible: no test wired a `ShotHistoryStorage` into an `AIManager`, so `shotHistoryStorage()` was null in every test and the whole resolve block was **unreachable by the suite**. Whatever went red, it was not that mutation reaching that code. The block could have been deleted without a single test failing.

  Fixed rather than re-worded: `loadFromStorageForgetsTurnIdsThatNoLongerNameAShot` and `loadFromStorageLeavesTurnIdsAloneWhenTheDatabaseCannotAnswer` now wire a real storage, and both assert the fixture actually loaded so "0 turns" can never read as a pass.

  Mutations that DO hold, each re-run:
  - Dropping the merge pre-read guard: `failed_pre_read_aborts_without_writing` red.
  - `remapTurnShotIds` keeping an unmapped source id: red on a turn still carrying source id **1096** — the reported defect reproduced. (Earlier attributed to the wrong slot: that id lives in `aTurnWhoseShotDidNotComeAcrossLosesItsShotId`, not in the map-shape slot.)
  - `existingShotIds` returning an empty `QSet` instead of `nullopt` when not ready: **two** slots red — `existing_shot_ids_answers_exactly_and_refuses_when_it_cannot_look` on the type, and `loadFromStorageLeavesTurnIdsAloneWhenTheDatabaseCannotAnswer` showing the user-visible consequence (all four references deleted). Reverted; `grep -c MUTANT` is 0.

## 7. Verification and close-out

- [x] 7.1 Build via `mcp__qtcreator__build` and run the full suite via `mcp__qtcreator__run_tests` (scope `all`). Confirm Qt Creator's active project is this clone first — two clones exist and its active project has drifted mid-session before. **Result: 113 passed, 0 failed, 0 skipped in 41670 ms.** One pre-existing flake was fixed on the way: `tst_beanbaseclient::inFlightSupersedeEmitsSingleTerminalSignal` used `QTest::qWait(600)` as a gate for two events (the request reaching the wire, the terminal signal arriving) — a timer standing in for an event, which CLAUDE.md bans — and lost the race under parallel load. Now `QTRY_COMPARE_WITH_TIMEOUT` / `QTRY_VERIFY_WITH_TIMEOUT`, with a short explicit settle window kept only for the negative assertion (proving a second signal never arrives inherently needs one).
- [x] 7.2 Restore a real backup on a populated database and confirm: shots merge without duplication, conversation turns resolve to existing shots, and the summary states the pre-import count. **Done — see 10.1, 10.7, 10.8.**
- [x] 7.3 Run a LAN device migration selecting only AI conversations and confirm every imported turn's id is cleared rather than carried. **Done — see 10.10.**
- [x] 7.4 Update the wiki manual (`Kulitorum/Decenza.wiki.git`) — a restore can now fail where it previously reported success, which is user-visible. Three to five sentences: what the message means and that nothing was changed. Cut it by half before committing. **Done** — two sentences added under **Backup and Restore**, wiki commit `f38d97d`. Drafted at three sentences, cut to two.
- [x] 7.5 Run `openspec archive fix-restore-id-remap` as the last commit on the feature branch, before merge.

## 8. Review pass (PR #1853)

Five review agents ran against `4fbbec0f`. Every finding below was verified against the source before acting; the ones acted on are all real.

- [x] 8.1 **`existingShotIds` reproduced the defect this change exists to fix.** It returned an empty `QSet` both when the database was not ready and when the query failed, and the caller's response to "resolves nothing" is to DELETE. One transient `SQLITE_BUSY` would have stripped every advisor-to-shot link on the device. Now `std::optional<QSet<qint64>>`: `nullopt` means "could not answer", the caller leaves the data alone, and a read that stopped early returns `nullopt` too.
- [x] 8.2 **"In memory only … reversible" was false.** `loadFromStorage` mutates `m_messages` and `saveToStorage` writes `m_messages` verbatim, so the next save — including the one `AIManager::switchConversation` performs on the outgoing conversation — persists the drop. Both comments now say so. The behaviour is acceptable *because* 8.1 makes the drop conditional on a real answer.
- [x] 8.3 **`DataMigrationClient::m_shotImport` was never reset** while every sibling counter was. A second run (`importOnlyAIConversations()`, or a reconnect to a different source device) would remap that run's turns through the previous run's map — the same defect one layer up. Reset in `startImport()`.
- [x] 8.4 **`/api/backup/restore` reported a refused import as `success: true`**, and no client read `shotsRefused`. `success` now reflects whether shots that were attempted actually landed, a plain failure carries an `error`, and the page renders both as an error banner instead of a green "Restore complete".
- [x] 8.5 **"Nothing was changed." was untrue on the merge path**, where profiles, settings and media still restore. Scoped to "Your existing shots were not changed."
- [x] 8.6 **`m_pendingMetadataWrites` was a `QSet`**, but one reply drives two writes for the same shot id — the first outcome consumed the entry and the second, possibly the failing one, was discarded as "not ours" by the filter added to catch failures. Now a `QHash` refcount, with a test.
- [x] 8.7 **`shotMetadataCaptureFailed` had no consumer.** Wired to the existing storage-error toast in `main.qml` with advisor-specific wording, because the generic "please try again" is wrong advice for a shot that does not exist.
- [x] 8.8 **Dead fields.** `ImportResult::referencesRemapped` / `referencesCleared` were written only into a by-value lambda copy and read nowhere; `ImportTally` already owned those counts. Deleted, along with the `mutable` that existed solely for them. `ImportTally`'s are renamed `turnsRemapped` / `turnsCleared` — they count turns while its other fields count conversations.
- [x] 8.9 **Counters disagreed with the map on the replace-mode abort path**, while a comment asserted they could not. `imported`/`skipped`/`failed` are now bound to the tally rather than copied into it at the success tail, and the copy-out comment names the two early returns that genuinely bypass it.
- [x] 8.10 **`destShotsBefore` is `std::optional<int>`.** It is only measured in merge mode, and a bare 0 in replace mode was printed as "destination held 0 before" against a populated database — a new false statement replacing the old ambiguous one.
- [x] 8.11 **Pre-existing silent failures in the touched function**, per the fix-what-you-touch rule: a failed samples query read as "this shot has no samples" (shot imports, no graph, nothing says why); a failed phases query used as a schema probe (a locked database read as "old schema", every phase imported with an empty reason); `lastInsertId()` unchecked, which could put a mapping to 0 in the map — an entry that exists and names nothing.
- [x] 8.12 **Conversations skipped or malformed were counted nowhere**, so "3 imported" was the whole story when 40 arrived. `ImportTally` gained `conversationsSkipped` and `conversationsMalformed`.
- [x] 8.13 **The backup endpoint dropped conversations when the server was destroyed mid-restore.** Moving the import into the continuation put it below the `*destroyed` guard, where the old in-loop write had survived. They are now written (without a map) before that early return.
- [x] 8.14 **A plain shot-import failure in LAN migration fell through both arms silently** and the dialog printed "Import complete". Split into refusal / plain failure / missing storage.
- [x] 8.15 **Comment corrections.** A cross-reference to `remapConversationShotIds`, a function that does not exist; a `joinErrors` rationale describing a failure `translateString` cannot produce; "nothing listened" (`ShotHistoryExporter` has been connected since before this change — nothing acted on the FAILURE); a warning asserting one cause where five produce it; "opened with 1058 seconds earlier", which does not parse and is the evidentiary basis for both guards; an unsourced "8.2 MB" among figures that all state their method; and the 0.178 ms median outlier, now recorded as unexplained rather than explained away. The header claim that a stale id "is never read back as live" is narrowed: `loadRecentAssistantTurnsForKey` reads raw storage and does not go through the repair.
- [x] 8.16 **Tests.** Five slots added, no new file: `existingShotIds` exact-answer and refusal; the read-time repair with a real storage wired (previously unreachable — see 6.5); the leave-alone path when the database cannot answer; a rating written to a missing shot reporting failure; and two writes to one shot both reporting. The weak `source_ids_beyond_destination_are_all_remapped` slot was folded into the map slot as a single identity assertion, and task 6.4's actual requirement — a REAL `ImportResult.shotIdMap` fed into `importConversationsStatic`, then read back from storage — is now `aRealImportMapCarriesStoredTurnsOntoDestinationIds`.

## 9. Second review round (PR #1853, on `8d0b4229`)

Five agents again. Round 2's job was to check whether round 1's fixes landed and what they broke. Two of the round-1 fixes were themselves defective, and one of them was worse than the code it replaced.

- [x] 9.1 **A refused import no longer imports the conversations at all.** All three surfaces treated "refused" as "no shots came with these" and imported with every id cleared. But the guards fire on a TRANSIENT condition, so the user's next move is to retry — and the cleared conversations have already consumed their keys, so the retry skips them as duplicates and the linkage is gone permanently. The user does everything right and still loses it. `ImportResult::refused()` now gates the import; the archive is left intact for the retry.
- [x] 9.2 **The read-time repair was being undone by `saveToStorage`'s reconcile branch.** When another writer has appended to the same key — the MCP `ai_advisor_invoke` path — the branch adopts the on-disk array verbatim, and that array still carries the stale ids. Dropped ids are now remembered in `m_forgottenShotIds` and re-applied after reconciliation. Three comments and a test rested on the false "writes `m_messages` verbatim" claim.
- [x] 9.3 **The repair never ran for the conversation restored at startup.** `AIManager` loads the most recent conversation in its constructor, before `MainController` wires the storage, so the guard was false and the one conversation the user is most likely to continue loaded unrepaired every launch. Round 1 recorded this as a performance benefit ("free today"), which is how a functional hole stops being re-examined. Extracted as `AIConversation::repairStaleTurnShotIds()` and called again from `setShotHistoryStorage`.
- [x] 9.4 **The `*destroyed` rescue added in round 1 was deleted.** It wrote the stashed conversations with a NULL id map while `success` and `shotImport` were both in scope — stripping links the map beside it could have preserved — and a written conversation consumes its key, so it was worse than the bare `return` it replaced. It could not run in any case: the continuation is delivered queued to `this`, and `~QObject` removes posted events for a destroyed receiver.
- [x] 9.5 **The null-map decision is one definition.** `ImportResult::idMapOrNull()`. The three call sites each rested on a different local invariant, one of them holding only because of a gate 300 lines away in another function. The producer also clears `shotIdMap` on failure, so a rolled-back import cannot hand back a map naming ids the rollback erased — that makes all three callers correct by construction rather than by coincidence.
- [x] 9.6 **The JS `catch` arm still reported success.** The whole refusal check lives inside that `try`, so any parse failure landed on a green "Restore complete" and undid the round-1 fix one path over. The page now gates on the server's own `success` field rather than re-deriving the verdict, so the two representations cannot drift.
- [x] 9.7 **`hasShotsDb && !m_storage` answered `success: true`** — the archive carried a shot history, none of it landed, and the synchronous path reported a clean restore. Round 1 fixed only the async branch.
- [x] 9.8 **The migration dialog printed "Import complete" beside the error.** Round 1's commit message claimed this was fixed; it was not, it only added a second contradicting line. `importComplete` is now emitted only when nothing failed.
- [x] 9.9 **The `newId <= 0` guard added in round 1 was removed.** It `continue`d AFTER a committed INSERT, leaving a shot with no samples and no phases, counted `failed` while present in the database, absent from the map (which means "clear the reference" for a shot that exists), and skipping the rollback every other insert-side failure performs in replace mode. It made the log and the database disagree to guard a state `INTEGER PRIMARY KEY` forbids.
- [x] 9.10 **The generic toast was still being announced to screen-reader users.** `errorOccurred` ("please try again") is emitted immediately before `shotMetadataCaptureFailed`; the advisor message overwrote it visually but `AccessibilityManager.announce` had already spoken the wrong advice — assertively, to exactly the users who cannot see it replaced. Suppressed via `AIManager::hasPendingShotMetadataWrite()`.
- [x] 9.11 **`conversationsSkipped` / `conversationsMalformed` demoted to locals.** Round 1 deleted two `ImportResult` fields for being read only by their producer, and round 2 found I had added two more of the same shape one layer up. They now also catch a conversation whose `messages` is not an array, which used to import as an empty conversation and be reported as a success — a card that opens to nothing. `ImportTally::conversations` renamed `conversationsImported`, since every caller renamed it at the boundary.
- [x] 9.12 **Comment corrections.** The guard rationale still asserted the field-restore theory `design.md` retracts, pointing the next reader at a failing `SELECT` that document says not to look for. "Unreachable" was too strong for the old GUARD 2 (a read truncated at row 0 reaches it). The copy-out said a failure yields an empty map, which is true of an integrity refusal and false of a mid-import abort. `existingShotIds`'s doc listed two of its three `nullopt` causes. Two more stale comment blocks were left stacked above their replacements, and `#include <QSet>` in `aimanager.h` was dead. The test comment attributing the settings wipe to "Settings and AIManager both touch the store" named the wrong mechanism — it is `clearAllConversationsOnce`, correctly named in four other comments in that same file.
- [x] 9.13 **Tests.** `twoWritesToOneShotBothReportTheirOutcome` now drives both real producers instead of poking the refcount, and asserts the premise (one reply, two writes, same shot) — as written it would have stayed green if the bean-correction producer stopped registering at all, which was its only coverage. Added `anExistingConversationIsSkippedWholeNotOverwritten` (the shared importer's skip-existing branch was dead in the suite — every slot cleared settings first — on code just collapsed from three copies) and `aRepairedConversationStaysRepairedAcrossAReconcilingSave` for 9.2. The e2e slot now asserts `destShotsBefore == 60` so its literal padding constant is self-checking.
- [x] 9.14 **Mutation-checked.** Bean-correction producer stops registering → `twoWrites…` red at 1-vs-2. `saveToStorage` stops re-applying the drop → `aRepairedConversation…` red with stale id **987654** restored, the exact defect. Third mutation — producer stops clearing `shotIdMap` on failure — was **NOT** caught, and that is recorded rather than papered over: both guards fire before the first INSERT, so the map is empty on a refusal whatever that line does. The state it guards is a replace-mode INSERT failure part-way through, which needs one row to succeed and a later one to fail; no honest fixture produces that. The line stays and is documented as untested at the assertion that does not cover it.

## 10. Live verification, and what it turned up

Run on the maintainer's Mac against his real database (1135 shots, 18.7 MB).

- [x] 10.1 **7.2 — merge restore onto a populated database.** `destination held "1135" before; 0 imported, 1135 skipped, 0 failed`, and `1135 shot(s); 1135 matched for de-duplication` — the rewritten equality guard passing on real data. Row count and id range unchanged. The new summary line reports the pre-import count that the old one could not.
- [x] 10.2 **Replace restore.** `destination held "(not measured, replace mode)" before` — the `std::optional<int>` doing its job; the old code would have printed `0` against a database that held 1135.
- [x] 10.3 **The reported defect reproduced live.** A replace restore moved every shot from ids 1..1172 to **1173..2307** — shot `275d16f4` went 1172 → 2307. Same mechanism as the 2026-08-21 incident, on demand.
- [x] 10.4 **Restore no longer renumbers.** Prompted by the maintainer asking why we renumber at all, which had no good answer: the import simply omitted `id` from its INSERT, and replace mode clears with `DELETE`, which does not reset `sqlite_sequence` — so restoring a backup into the database it came from moved every row to a fresh block for nothing. An imported shot now keeps its own id wherever that id is free; replace mode also realigns the sequence. The id map remains, and remains necessary for genuine collisions and cross-device migration, but is now usually an identity. **This addresses the defect one level below where the rest of this change works** — it removes most of the renumbering rather than following it.
  - A first attempt was wrong and the new test caught it: letting `AUTOINCREMENT` place a relocated row meant an early collision consumed id 3 before the shot that owned id 3 arrived, so a free id was renumbered anyway. Relocations now go above every id in use in either database.
- [x] 10.5 **The `beanbase_id` backfill had been failing on every import.** Five rows hold `beanbase_json = ''`, and `json_extract('', '$.id')` is a hard error that aborts the whole `UPDATE` — so none of the legitimate rows were ever backfilled, marked only by a `qWarning`. Guarded with `json_valid()`. Found because live testing surfaced the warning; it is pre-existing, in a function this change rewrites.
- [x] 10.6 **The restore dialog's merge switch was misleading.** The label was a status line ("Merge with existing data") and the switch beside it was `checked` for **replace**, so an unchecked switch next to the word "Merge" meant a merge was about to run. The maintainer misread his own dialog during this testing — on a control that can delete every shot on the device — and reported a "clean restore" that the log shows was a merge. The `accessibleName` was correct, so a screen-reader user got the right answer while a sighted user did not. Label is now static and the switch means what it says; default unchanged.
- [x] 10.7 **Replace restore with id preservation, on the real database.** All 1135 id→uuid pairs came back **byte-identical** to the pre-test snapshot (ids 1–1172, `sqlite_sequence` 1172, shot `275d16f4` back at 1172 from 2307), and every sample and phase row still joins (1135/1135, 3757/3757). The restore is now a restore rather than a re-import — it also undid the renumbering the earlier test run had caused.
- [x] 10.8 **The startup repair caught a real orphaned reference, unprompted.** A planted fixture was attempted first and was a **no-op**: QSettings maps `/` to `.` in the macOS plist, so `defaults write "ai/conversations/…"` writes keys the app cannot see. What the repair actually fired on was one of the maintainer's own conversations (`b8efc1a7…`, 13:33 that day) holding `shotId: 2288` — an id that existed only inside the window where an earlier test restore had renumbered the database to 1173–2307. The next restore put the ids back and orphaned the reference. `repairStaleTurnShotIds` dropped both dead references at startup, via exactly the deferred path 9.3 added (`no shot storage yet … deferring until it is wired`, then the drop once `setShotHistoryStorage` arrived).

  **That is the reported defect, reproduced end to end on real data, and it is the strongest evidence for 10.4**: renumbering a shot silently breaks every outside reference to it, and the advisor conversation is one. The reference was recovered — the renumbered import assigned ids sequentially from 1173 in rowid order, so 2288 was the 1116th row, source id **1152**, shot `c4575a14`, profile `D-Flow / Q`, which matches that conversation's own context label. Rewritten 2288 → 1152; relaunch then logged no drop.
- [x] 10.9 **A clean repair now says so.** The deferral announces that a check is coming and the success path logged nothing, so a submitted log could not distinguish "ran and everything resolved" from "never ran" — the state this method exists to fix. Added the counterpart line.
- [x] 10.10 **7.3 — LAN migration, AI conversations only, from the tablet.** `5 conversation(s) imported, 0 already present, 0 malformed; 0 shot reference(s) remapped, 36 cleared (no shot import accompanied them — all ids cleared)`. Verified against the plist rather than the log: all 5 imported conversations hold 56 turns with **zero** shotIds, and the Mac's own 3 conversations kept their valid local ids (1152/1155/1152) untouched. Both halves of the requirement — an id naming a database this device does not have is cleared, and an existing local reference is not disturbed. Without this, 36 tablet ids would have been written verbatim and resolved against this Mac's own 1135 shots, attributing advice to unrelated coffee.
